"use client"

/* ─── BOTTOM-NAVIGATION — AUSSER BETRIEB (19.08.2026) ─────────────────────────
   Die App zeigte gleichzeitig zwei Navigationen: den Hamburger oben rechts und
   diese feste Leiste unten. Auf dem Desktop stand sie zusaetzlich quer ueber
   das ganze Fenster, obwohl sie fuer ein Handy gedacht war.

   Entscheid aus dem Briefing: ein Navigationssystem, und zwar der Hamburger
   oben rechts — auf jedem Geraet. Diese Leiste bleibt als Datei bestehen, damit
   die zwei Dutzend Seiten, die sie einbinden, unveraendert weiterlaufen; sie
   zeichnet aber nichts mehr.

   Wer sie je zurueckholen will: die alte Fassung liegt in der Git-Historie. */

export default function BottomNav() {
  return null
}
